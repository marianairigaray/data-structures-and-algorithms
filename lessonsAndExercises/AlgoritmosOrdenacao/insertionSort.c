#include <stdio.h>
#include <stdlib.h>

int main()
{



}


void insertionSort(int array[], int size)
{
    int j, key;

    // pulamos o primeiro elemnteo pois ja consideramos ele ordenado

    for (int i = 1; i < size; i++)
    {
        key = array[i];
        j = i - 1;

        while (j > 0 && array[])
        {
            /* code */
        }
        
    }
}